server {
    listen 443 ssl;
    server_name api.noskillworld.fr;

    location / {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}