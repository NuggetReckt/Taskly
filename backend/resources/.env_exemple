# Database credentials
DB_USERNAME="" # Database username credential, set on "user" if using on docker
DB_PASSWORD="" # Database password credential, set on "password" if using on docker
DB_DBNAME="" # Database name, set on "taskly" if using on docker
DB_HOST="" # Host of the database, set on "db" if using on docker

# API credentials
API_SECRET="" # REST API secret
JWT_SECRET="" # jwt secret key
JWT_ALGORITHM="" # jwt encoding algorithm, "using HS256" is recommanded
